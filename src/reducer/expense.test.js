/// api call: http://localhost:2000/api/expenses

import * as constants from '../actions/constants';
import ExpenseReducer from './expense.reducer';

describe('expense reducer', () => {

    it('send the data to server', () => {
        
    })
});